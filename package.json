{
  "name": "babylon",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate:client": "openapi-generator-cli generate -i openapi.json -g typescript-axios -o common/src",
    "generate:backend": "openapi-generator-cli generate -i openapi.json -g typescript-nestjs -o backend/src/generated"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.2.5"
  }
}
